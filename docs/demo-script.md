# MetricThread public demo script (target: 2 minutes 40 seconds)

Use the deployed Vercel judge-demo URL. Keep the visible **SYNTHETIC LIVE SIMULATION** label on screen whenever data is shown. This script is intentionally conditional on the required funded GPT-5.6 verification: do not say GPT-5.6 generated a result until that check has actually passed and is documented.

| Time | On screen | Narration |
| --- | --- | --- |
| 0:00–0:15 | Title and status rail | “MetricThread is an Enterprise Intelligence Agent for a VP of Growth. It watches signals across Client, Financial, and Partner data, but every result here is a synthetic live simulation.” |
| 0:15–0:35 | Start simulation; show metric cards update | “A compressed day of nine business events arrives every five seconds. Redis Streams send each event to both a hot dashboard path and a durable analytical path.” |
| 0:35–1:05 | Evidence table; open primary signal | “The deterministic engine aligns 180 days of data, prepares non-stationary series, selects model history by BIC, and corrects the whole candidate family. This card says partner referral quality is predictive of CAC—not that it causes CAC. You can inspect its q value, effect size, sample size, confidence components, and evidence ID.” |
| 1:05–1:30 | Insight, Explain Why, recommendation | “Only accepted evidence can reach the reasoning layer. [After the funded check: ‘GPT-5.6 received this compact evidence packet and produced this cited narrative.’] The stored confidence remains deterministic, and the recommendation is human-controlled: proposed, planned, then implemented.” |
| 1:30–1:55 | Ask “Why is CAC rising?” then unsupported competitor question | “The grounded chat returns the stored insight and signal IDs for the CAC question. An unsupported competitor-pricing question refuses with no evidence instead of inventing an answer.” |
| 1:55–2:20 | +10%, seven-day scenario | “For a limited what-if, I can change marketing spend by up to 20% for one to seven days. This forecast is deterministic and back-tested; it shows a baseline, prediction interval, assumptions, reliability, and its supporting signal ID. It is not a causal promise.” |
| 2:20–2:40 | Read-only banner and repository README | “The public judge demo is read-only, so evidence and scenarios are safe to explore without changing the seed. Codex was used to build the streaming pipeline, statistical checks, test suite, deployment setup, and documentation. The repository documents the model boundary, the complete evidence trail, and how to reproduce the synthetic demo.” |

## Recording checklist

- [ ] Public Vercel URL and Render API rehearsal pass before recording.
- [ ] Browser console is clean and the synthetic label is visible.
- [ ] Live GPT-5.6 generation has passed only if that claim is spoken or shown.
- [ ] Audio explicitly explains both Codex use and GPT-5.6 use, as required by the challenge.
- [ ] Public YouTube visibility is selected and final duration is under three minutes.
- [ ] Video description links to the public repository and deployment.
